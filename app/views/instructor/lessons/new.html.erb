<br />
<div class="booyah-box col-10 offset-1 feature">
  <h1>Add a New Lesson</h1>
  <%= simple_form_for @lesson, 
  url: instructor_section_lessons_path(@section) do |f| %>
    <%= f.input :title %>
    <%= f.input :subtitle %>
    <%= f.submit "Add Lesson" %>
  <% end %>
</div>